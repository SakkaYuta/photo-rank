# Git secrets detection patterns - minimal to avoid false positives
# Only detect actual secrets with values, not code patterns

# Supabase keys with actual values (not in code comments)
eyJ[a-zA-Z0-9_.-]{100,}

# Stripe live keys only
pk_live_[a-zA-Z0-9]{24,}
sk_live_[a-zA-Z0-9]{24,}

# AWS live credentials only
AKIA[0-9A-Z]{16}

# Actual connection strings with passwords
://[^@/]+:[^@/]{8,}@[^/]+/